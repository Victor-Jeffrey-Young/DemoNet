{
  "version": "2.0.0",
  "tasks": [
    // =========================================================
    // Docker 基础设施（MySQL / Redis / RabbitMQ）
    // =========================================================
    {
      "label": "🐳 Docker: 启动",
      "type": "shell",
      "command": "docker compose up -d && echo '等待 MySQL 就绪...' && until docker exec demonet-mysql mysqladmin ping -uroot -p${MYSQL_ROOT_PASSWORD:-changeme} --silent 2>/dev/null; do sleep 1; done && echo '✅ MySQL/Redis/RabbitMQ 已就绪'",
      "options": { "cwd": "${workspaceFolder}" },
      "presentation": { "reveal": "always", "panel": "dedicated", "clear": true },
      "problemMatcher": []
    },
    {
      "label": "🐳 Docker: 停止",
      "type": "shell",
      "command": "docker compose down",
      "options": { "cwd": "${workspaceFolder}" },
      "presentation": { "reveal": "always", "panel": "dedicated" }
    },

    // =========================================================
    // 后端：编译
    // =========================================================
    {
      "label": "🔧 后端: 编译",
      "type": "shell",
      "command": "mvn clean compile -q",
      "options": { "cwd": "${workspaceFolder}/backend" },
      "group": "build",
      "presentation": { "reveal": "always", "panel": "dedicated", "clear": true },
      "problemMatcher": []
    },

    // =========================================================
    // 后端：开发模式（热重载 + Debug 端口 5005）
    // =========================================================
    {
      "label": "🔧 后端: 启动 Dev",
      "type": "shell",
      "command": "mvn spring-boot:run -Dspring-boot.run.jvmArguments=-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5005",
      "options": { "cwd": "${workspaceFolder}/backend" },
      "presentation": { "reveal": "always", "panel": "new", "clear": true, "showReuseMessage": true },
      "problemMatcher": []
    },

    // =========================================================
    // 前端：开发模式（Vite HMR）
    // =========================================================
    {
      "label": "🎨 前端: 启动 Dev",
      "type": "shell",
      "command": "npm run dev",
      "options": { "cwd": "${workspaceFolder}/frontend" },
      "presentation": { "reveal": "always", "panel": "new", "clear": true, "showReuseMessage": true },
      "problemMatcher": []
    },

    // =========================================================
    // 一键启动：Docker → 后端 + 前端（自动分终端）
    // =========================================================
    {
      "label": "🚀 启动全部",
      "dependsOn": [
        "🐳 Docker: 启动",
        "🔧 后端: 启动 Dev",
        "🎨 前端: 启动 Dev"
      ],
      "dependsOrder": "sequence",
      "group": { "kind": "build", "isDefault": true },
      "presentation": { "reveal": "never" },
      "problemMatcher": []
    }
  ]
}
