spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demonet_test?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: testpass
  redis:
    host: localhost
    port: 6379
  rabbitmq:
    host: localhost
    port: 5672
  flyway:
    enabled: true
    locations: classpath:db/migration
app:
  jwt:
    secret: test-secret-key-for-ci-at-least-256-bits-long-0123456789abcdef
    expiration: 86400000
  steam:
    api-key: test-steam-key
  steamgriddb:
    api-key: test-sgdb-key
  tmdb:
    api-key: test-tmdb-key
  igdb:
    client-id: test-igdb-client
    client-secret: test-igdb-secret
  upload:
    dir: ./uploads
IGDB_CLIENT_ID: test-igdb-client
IGDB_CLIENT_SECRET: test-igdb-secret
