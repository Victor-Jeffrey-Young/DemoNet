# 部署、运维与测试

> Docker 部署、环境变量、测试策略、验收用例、上线清单与回滚。最后维护：2026-07-13。
>
> 本文合并了原部署运维和测试用例文档。标有“待修复”“待实施”或旧日期的内容保留为历史验收背景；当前 CI/CD 流程、生产部署限制与实际验证状态以 [11-CI-CD-GitHub-Actions.md](11-CI-CD-GitHub-Actions.md) 为准。

---

## 本地开发

### 环境准备

```bash
# 首次：生成 .env 文件
bash scripts/setup-env.sh

# 启动基础设施（MySQL + Redis + RabbitMQ）
docker compose up -d

# 后端 (:8080)
cd backend && mvn clean spring-boot:run

# 前端 (:5173)
cd frontend && npm run dev
```

| 服务 | 地址 |
|------|------|
| 前端 | http://localhost:5173 |
| 后端 API | http://localhost:8080 |
| Swagger UI | http://localhost:8080/swagger-ui/index.html |
| RabbitMQ 管理界面 | http://localhost:15672（admin/changeme） |
| 默认管理员 | admin / 见 `ADMIN_DEFAULT_PASSWORD` 环境变量 |

### docker-compose.yml（开发）

| 服务 | 镜像 | 端口 |
|------|------|------|
| MySQL | `mysql:8.0` | 3306 |
| Redis | `redis:7-alpine` | 6379 |
| RabbitMQ | `rabbitmq:3.12-management` | 5672 / 15672 |

> 启动时自动挂载 `schema.sql` 建表（`CREATE TABLE IF NOT EXISTS`，幂等安全）。

---

## 环境变量 (.env)

复制 `.env.example` 为 `.env` 并填入真实值。

### 基础设施

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `MYSQL_HOST` | localhost | 数据库主机 |
| `MYSQL_USER` | root | 数据库用户 |
| `MYSQL_PASSWORD` | changeme | 数据库密码（**生产必须修改**） |
| `MYSQL_ROOT_PASSWORD` | changeme | MySQL root 密码（**生产必须修改**） |
| `REDIS_HOST` | localhost | Redis 主机 |
| `RABBITMQ_HOST` | localhost | RabbitMQ 主机 |
| `RABBITMQ_USER` | admin | RabbitMQ 用户 |
| `RABBITMQ_PASS` | changeme | RabbitMQ 密码（**生产必须修改**） |

### 应用安全

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `JWT_SECRET` | （内置默认值） | JWT 签名密钥（**生产必须修改为 32 字节以上随机字符串**） |
| `ADMIN_DEFAULT_PASSWORD` | changeme | 管理员初始密码（**生产必须修改**） |
| `UPLOAD_DIR` | ./uploads | 文件上传目录 |

### 外部 API 密钥

| 变量 | 说明 | 获取方式 |
|------|------|----------|
| `STEAM_API_KEY` | Steam Web API Key | https://steamcommunity.com/dev/apikey |
| `TMDB_API_KEY` | TMDB API v3 | https://www.themoviedb.org/settings/api |
| `IGDB_CLIENT_ID` | Twitch IGDB Client ID | https://dev.twitch.tv/console |
| `IGDB_CLIENT_SECRET` | Twitch IGDB Client Secret | 同上 |
| `STEAMGRIDDB_API_KEY` | SteamGridDB（竖版封面） | https://www.steamgriddb.com/profile/preferences/api |

### 可选功能

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `TURNSTILE_SITE_KEY` | 空 | Cloudflare Turnstile 前端 key（空=跳过人机验证） |
| `TURNSTILE_SECRET_KEY` | 空 | Cloudflare Turnstile 后端验证 key |
| `INVITE_ONLY` | false | 是否开启邀请码注册（通过管理后台动态调整） |

> **注**：API 密钥也可通过管理后台「API 设置」标签页动态更新（存储在 `app_settings` 表），无需重启服务器。环境变量在服务启动时作为初始值，数据库中的值优先级更高。

---

## 生产部署

### docker-compose.prod.yml（5 服务）

| 服务 | 说明 |
|------|------|
| `db` | MariaDB 11（替代 MySQL 8.0，生产更稳定） |
| `redis` | Redis 7-alpine |
| `rabbitmq` | RabbitMQ 3-management-alpine |
| `backend` | Spring Boot JAR → `backend/Dockerfile` |
| `frontend` | Nginx 静态文件 → `frontend/Dockerfile` + `nginx.conf` |

### 构建与启动

```bash
# 首次：生成 .env（填入生产密码和 API 密钥）
bash scripts/setup-env.sh

# 构建 + 启动全部服务
docker compose -f docker-compose.prod.yml up -d --build

# 查看日志
docker compose -f docker-compose.prod.yml logs -f backend
```

### Nginx 配置要点

- 前端静态文件（`dist/`）由 Nginx 直接 serve
- `/api/` 路径反向代理到 `backend:8080`
- `/uploads/` 路径由后端 serve（或 Nginx 直接映射上传目录）
- 建议配置：gzip 压缩 + 静态资源缓存头 + `try_files $uri $uri/ /index.html`（SPA 路由支持）

### 生产 docker-compose 已知问题

`docker-compose.prod.yml` 缺少以下环境变量注入（待修复）：

```yaml
# 需要补充到 backend service 的 environment 中：
ADMIN_DEFAULT_PASSWORD: ${ADMIN_DEFAULT_PASSWORD:-changeme}
APP_STEAMGRIDDB_API_KEY: ${STEAMGRIDDB_API_KEY:-}
SPRING_REDIS_PASSWORD: ${REDIS_PASSWORD:-}
SPRING_RABBITMQ_USERNAME: ${RABBITMQ_USER:-admin}
```

---

## 测试策略

### 当前状态

| 层级 | 工具 | 状态 |
|------|------|------|
| 单元测试 | JUnit 5 + Mockito | ❌ 0 覆盖（仅 1 个启动存在性验证） |
| 集成测试 | Spring Boot Test | ❌ 待实施 |
| API 测试 | REST Assured / Postman | ❌ 待实施 |
| E2E 测试 | Playwright / Cypress | ❌ 待实施 |

### 关键测试场景（上线前必须覆盖）

**后端（JUnit + Mockito）**：
- `AuthService`：注册流程（保留用户名/邮件归一化/重复用户名/重复邮件）
- `AuthService`：登录流程（密码错误/账号封禁）
- `ItemService.listHotItems()`：热度公式排序（有/无 recommendations 的混合场景）
- `ItemService.mergeInfoJson()`：字段合并逻辑（现有字段保留/新字段注入）
- `AdminService.batchUpdateStatus()`：状态批量变更
- `TagService.ensureTag()`：幂等创建标签

**集成测试（Spring Boot Test + H2/Testcontainers）**：
- POST /api/auth/register → 注册成功，返回 token
- POST /api/auth/login → 登录成功
- GET /api/items → 分页列表正确
- POST /api/admin/items（ADMIN token）→ 创建内容，status=0
- PUT /api/admin/approve/{id}（ADMIN token）→ status→1

---

## 上线清单

### 必须完成（阻塞上线）

| # | 任务 | 状态 |
|---|------|------|
| 1 | 修改所有默认密码（MySQL/Redis/RabbitMQ/JWT/Admin）| ❌ 待完成 |
| 2 | 配置 HTTPS（Let's Encrypt / Cloudflare Proxy）| ❌ 待完成 |
| 3 | `.env` 确认生产环境全部变量填写 | ❌ 待完成 |
| 4 | 数据库备份脚本就绪 | ❌ 待完成 |
| 5 | `AdminController` 密码重置接口改为不返回明文密码 | ❌ 待修复（安全） |
| 6 | 上传接口添加 MIME 类型白名单校验 | ❌ 待修复（安全） |
| 7 | `AdminDashboard.vue` 修复 TypeIcon import | ❌ 待修复（已知 bug） |
| 8 | `schema.sql` 补全 invite_codes / app_settings 表 | ❌ 待修复 |
| 9 | 生产 docker-compose 补全缺失环境变量 | ❌ 待修复 |
| 10 | 至少核心 Service 有单元测试 | ❌ 待实施 |

### 建议完成（不阻塞但影响质量）

| # | 任务 | 优先级 |
|---|------|--------|
| A | Swagger UI 生产环境关闭或限访 | P1 |
| B | Detail.vue 拆分（1315行过胖） | P1 |
| C | AdminController 中 JdbcTemplate 直接操作迁移到 Service | P2 |
| D | ECharts 懒加载（仅咖啡页加载） | P2 |
| E | Flyway 迁移管理（schema 版本化） | P2 |
| F | 替换占位图片为真实素材 | P1 |

---

## 回滚方案

```bash
# 停止当前服务
docker compose -f docker-compose.prod.yml down

# 恢复数据库（如有备份）
mysql -u root -p demonet < backup_YYYYMMDD.sql

# 启动上一版本镜像（git checkout + rebuild）
git checkout <上一个稳定 tag>
docker compose -f docker-compose.prod.yml up -d --build
```

---

## 监控建议

上线后渐进计划：

| 时间 | 任务 |
|------|------|
| 即刻 | 确认 5 个 Docker 服务全部 healthy；查看 `/api/admin/stats` 返回正常 |
| 24h | 检查后端日志无 ERROR；API 响应时间 P99 < 1s |
| 1 周 | 手动录入真实数据，替换种子数据；检查图片上传路径持久化 |
| 2 周 | 根据实际访问模式调整 Redis TTL；检查 RabbitMQ 队列无堆积 |
| 1 月 | 接入更多外部 API；考虑 Elasticsearch 替换 LIKE 全文搜索 |


---

# 验收测试用例附录

> 以下内容保留原测试场景、步骤与预期结果。执行结果、覆盖范围和测试工具的当前状态以本文件的“测试策略”及 CI/CD 文档为准。

# DemoNet - 测试用例

本文档记录了项目第一阶段（前端管理后台重构）与第二阶段（后端架构稳定性与性能优化）的核心测试用例，用于验证重构后的系统是否符合预期。

---

## 阶段一：前端管理后台 UI/UX 重构测试用例

### 1. API 密钥与系统设置重构测试
| 测试编号 | 测试场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **FE-01** | **API 设置面板布局测试** | 1. 登录管理后台，进入最后一栏的“API 设置”<br>2. 观察大屏显示效果<br>3. 缩小浏览器窗口或使用手机模拟器 | 大屏下“第三方 API 密钥”与“邀请码机制”呈现左右并排的卡片布局（`grid-cols-2`）；窄屏/移动端自动垂直堆叠（`grid-cols-1`）。无内容溢出。 |
| **FE-02** | **全局开关外置显示** | 1. 在 API 设置面板查看邀请码卡片头部 | “启用邀请注册”的全局 Switch 开关直接内嵌在卡片标题栏右上角，且支持点击实时切换和保存状态。 |
| **FE-03** | **Iconify 图标替换** | 1. 浏览 API 设置的各项标题<br>2. 浏览内容管理列表的空状态 | 原生 Emoji 已被系统集成的 `@iconify/vue` 矢量图标替代（如钥匙、卡券、空箱子），图标颜色带有与主题融合的柔和光晕，且不再显得突兀。 |
| **FE-04** | **邀请码状态徽标** | 1. 在 API 设置中点击生成新的邀请码<br>2. 使用其中一个邀请码注册新用户<br>3. 返回 API 设置刷新 | 未使用的邀请码显示绿色的“全新可用”；已使用的邀请码显示红色的“已消耗”并显示注册人。 |

### 2. 列表视图移动端兼容性测试
| 测试编号 | 测试场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **FE-05** | **用户管理列表移动端滚动** | 1. 切换到移动端视图<br>2. 进入“用户管理”菜单<br>3. 尝试左右滑动表格 | 表格可顺滑横向滚动，右侧的“操作”列（编辑/封禁/删除等）不再强制悬浮遮挡数据，滑动到最右侧时自然显示。 |
| **FE-06** | **内容管理列表移动端滚动** | 1. 切换到移动端视图<br>2. 进入“内容管理”，点击切换为“列表模式”<br>3. 尝试左右滑动表格 | 与用户管理一致，“编辑”与“删除”等操作按钮不再溢出覆盖表格内容，支持平滑横滑。上下架 Switch 组件居中且正常工作。 |

---

## 阶段二：后端稳定性与性能优化测试用例

### 1. 消息队列与缓存稳定性
| 测试编号 | 测试场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **BE-01** | **Redis 连接池防阻塞** | 1. 使用压测工具（如 JMeter 或 Apache ab）对任意使用缓存的接口并发发送 200 个请求 | Redis 连接稳定分配（Lettuce 激活多线程处理），接口无 `RedisConnectionFailureException`，平均响应时间波动较小。 |
| **BE-02** | **RabbitMQ 死信队列与重试机制** | 1. 故意向 RabbitMQ 队列发送格式错误或无法处理的消息体（例如损坏的抓取任务）<br>2. 观察控制台日志及 RabbitMQ 管理面板 | 消费失败一定次数后，消息不再陷入死循环无休止消耗 CPU，而是被路由转移至 `dlx` 死信交换机并停留在死信队列中等待手动排查。 |
| **BE-03** | **IGDB Token 缓存共享** | 1. 启动多个后端实例（或多次调用获取 IGDB Token 的方法） | 仅在第一次调用或 Token 即将过期时向 Twitch API 发起 HTTP 请求获取 Token，后续请求均直接从 Redis 获取，且多个实例共享同一个 Token。 |

### 2. N+1 瓶颈与 SQL 性能优化
| 测试编号 | 测试场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **BE-04** | **AdminService 批量更新优化** | 1. 在管理后台勾选 100 条内容数据<br>2. 执行“批量上架/下架”操作<br>3. 监控后端打印的 SQL 语句 | 只打印 1 条执行语句：`UPDATE items SET status = ? WHERE id IN (...)`，而不再是 100 条单独的 `UPDATE` 和 `SELECT` 语句。操作耗时降至毫秒级。 |
| **BE-05** | **ReviewService 统计聚合优化** | 1. 对某个 ID 的条目（itemId=100）插入 5000 条测试评论数据<br>2. 调用 `/api/public/items/100` 或直接请求 `ReviewService.stats` | 只产生 1 条 SQL 语句 `SELECT COUNT(*), AVG(rating)...`，内存占用平稳，不触发大规模 GC，耗时在 10ms 以内（相比之前取出 5000 个 Java 对象的数百毫秒耗时大幅缩短）。 |

### 3. 第三方接口并发抓取优化
| 测试编号 | 测试场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **BE-06** | **Steam 批量同步并发抓取** | 1. 触发 `SteamService.updateExistingGames()` 批量同步，或调用包含多个 DLC 的 Steam 游戏详情抓取 | 多个网络请求在 `CompletableFuture` 自定义线程池（大小 10）下并行执行，控制台同时打印多条处理日志，整体响应时间接近最慢单个请求的时间，而不是所有请求时间的累加。 |
| **BE-07** | **Steam 海报多重检测与缓存** | 1. 请求某个 Steam 游戏的海报图片（调用 `resolvePosterUrl`）<br>2. 多次请求相同的游戏 | 首次需要多次 HEAD 请求探测可用 CDN，解析出最终海报后，结果将写入 Redis 缓存。后续在 7 天内重复抓取时，瞬间返回缓存的海报链接。 |
| **BE-08** | **TMDB 搜索结果并发详情抓取** | 1. 在后台新增内容中搜索 TMDB 影视关键字（如“Matrix”）<br>2. 抓取列表渲染耗时 | 搜索获取到 10-20 条结果后，由于并发获取每一条记录的详情（`fetchMovieDetail` / `fetchTVDetail`），搜索列表加载速度相比之前的串行循环抓取提升 80% 以上。 |

---

## 阶段三：核心瓶颈突破与资源优化测试用例

### 1. 内存及 OOM 隐患消除
| 测试编号 | 测试场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **BE-09** | **大字段排除测试 (Items)** | 1. 请求 `/api/items/hot` 或 `/api/items/featured` 等列表接口<br>2. 检查响应的 JSON 负载 | 返回的 Item 列表中仅包含 `id`, `title`, `cover_url` 等必要字段，不再包含臃肿的 `description` 和 `info_json` 字段。单次请求的数据体积从几 MB 降至几十 KB，彻底消除 OOM 风险。 |

### 2. 数据库索引与安全加固
| 测试编号 | 测试场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **BE-10** | **关键索引验证** | 1. 检查数据库结构（如执行 `SHOW INDEX FROM reviews;`） | 成功应用 `V2__Add_indexes.sql` 脚本，表内正确生成了 `idx_reviews_item`, `idx_reviews_user`, `idx_items_status` 等关键索引，有效避免十万级数据下的全表扫描。 |
| **BE-11** | **防 SQL 注入验证** | 1. 检查 `ItemService` 与 `TagService` 中的自定义 SQL | 已完全废除原有的字符串拼接（如 `replace("'", "''")`），改为安全的 `?` 占位符与预编译参数绑定（`PreparedStatement`），阻断 SQL 注入路径。 |
| **BE-12** | **AdminController JSON 处理** | 1. 在后台进入“阅读条目”详情，上传或更新一张封面图片<br>2. 检查数据库中该条目的 `info_json` | `reader_url` 属性能够被正确写入或覆盖，且未破坏该字段内的其他 JSON 结构（已弃用危险的正则篡改，改用成熟的 `ObjectMapper` 解析机制）。 |

### 3. 高可用防护机制
| 测试编号 | 测试场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **BE-13** | **RestClient 全局超时阻断** | 1. 断开网络或人为制造第三方（Steam/TMDB）极高延迟<br>2. 触发一次第三方条目搜索 | 强制在 5 秒（连接建立）或 15 秒（读取等待）后主动抛出超时异常并结束请求，杜绝无期限等待导致 Tomcat 线程池耗尽（假死）。 |
| **BE-14** | **文件上传上限防御** | 1. 尝试上传一个超过 10MB 的测试文件 | 服务端在入口层即刻拦截并返回 `413 Payload Too Large`，验证 `max-file-size=10MB` 配置已生效，封堵大文件恶意挤占磁盘与内存的漏洞。 |

---

## 阶段四：系统细节优化与底层防线巩固测试用例

### 1. Redis 与序列化安全加固
| 测试编号 | 测试场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **BE-15** | **Redis 反序列化白名单拦截** | 1. 尝试将伪造的带有恶意 `@class`（如 `java.lang.Runtime` 等非项目类）的 JSON 缓存数据写入 Redis<br>2. 触发对应的读取接口 | 系统抛出反序列化类型不允许的异常（由 `BasicPolymorphicTypeValidator` 拦截），成功防御潜在的 RCE（远程代码执行）攻击。 |
| **BE-16** | **自定义缓存 TTL 策略验证** | 1. 触发 `hotItems` 和 `visibleTypes` 的查询接口以生成缓存<br>2. 登录 Redis（或使用监控工具）查看 TTL | `hotItems` 的 TTL 显示不超过 5 分钟，而 `visibleTypes` 的 TTL 显示长达 1 天（86400秒），不再是全局僵化的 10 分钟。 |

### 2. 对象复用与 CPU 性能优化
| 测试编号 | 测试场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **BE-17** | **JWT 验证开销优化** | 1. 对受保护的接口发起并发压力测试（模拟高频校验 Token） | `JwtAuthFilter` 现在针对每次请求仅解密、解析一次 JWT 签名提取 `Claims`，消除了原本 3 次重复解密的性能浪费，CPU 负载显著降低。 |
| **BE-18** | **ObjectMapper 实例复用** | 1. 执行包含第三方数据合并更新的操作（频繁触发 `ItemService.mergeInfoJson`） | 业务逻辑复用了 Spring 容器注入的 `ObjectMapper`，不再局域实例化重量级对象，避免了高并发下的短命对象激增及 Minor GC 卡顿。 |
| **BE-19** | **SteamGridDB 密钥内存缓存** | 1. 触发后端后台批量同步 Steam 游戏海报的操作 | `SteamGridDBService` 只会在首个请求时触发 1 次 `SELECT` 获取 API 密钥，在随后的 5 分钟内数以百计的调用均直接从内存返回，免除冗余的数据库 I/O。 |

### 3. 数据完整性与带宽优化
| 测试编号 | 测试场景 | 操作步骤 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **BE-20** | **静态资源浏览器强缓存** | 1. 打开开发者工具 (Network)<br>2. 访问携带 `uploads` 目录下图片的页面<br>3. 刷新页面 | 首次请求返回 200，后续刷新图片请求显示 `(disk cache)` 或 `(memory cache)` 或返回 304。由于 `WebConfig` 配置了 `86400`，图片被正确缓存在本地，降低了服务器出口带宽压力。 |
| **BE-21** | **外键级联与脏数据清理** | 1. 执行后台删除某个 User（包含他留下的评论和收藏）<br>2. 查阅关联表 `reviews` / `user_items` | `V3__Add_foreign_keys.sql` 成功执行并清理了历史脏数据。得益于 `ON DELETE CASCADE`，主表数据删除后，从表的孤立评论/收藏记录被数据库原生机制自动同步销毁，保证数据绝对一致性。 |
