# 阶段 8：平台基建补齐

> **状态**：✅ 完成（2026-05-29）

---

## 任务清单

| # | 任务 | 说明 | 状态 |
|---|------|------|------|
| 8.1 | 评论系统 | `ReviewController` + `ReviewSection.vue` + `Review` Entity/Mapper/Service | ✅ |
| 8.2 | Redis 缓存 | `@Cacheable` hot/featured/recommended，TTL 10min + `CacheErrorHandler` 降级 | ✅ |
| 8.3 | Swagger/SpringDoc | `springdoc-openapi`，访问 `http://localhost:8080/swagger-ui/index.html` | ✅ |
| 8.4 | 定时抓取 | `ScheduledFetchService`（框架就绪，待接入 Steam/TMDB 具体抓取逻辑） | ✅ |
| 8.5 | Docker 部署 | `backend/Dockerfile` + `frontend/Dockerfile` + `nginx.conf` + `docker-compose.prod.yml` | ✅ |
| 8.6 | 管理员上传竖版图 | `poster_url` 字段 + 上传接口（之前已有） | ✅ |

## 详细说明

### 8.1 评论系统
- 表 `reviews` 已有（item_id, user_id, content, rating, created_at）
- 需要 ReviewController (GET/POST /api/reviews)
- 前端 ReviewSection.vue 组件
- 支持评分 + 文字评论

### 8.2 Redis 缓存
- 热门列表：`items:hot`
- 推荐列表：`items:recommended`
- 搜索结果：`search:{query}`
- 使用 Spring `@Cacheable` 注解 + RedisTemplate

### 8.6 管理员上传竖版图
- GameCard 目前缺竖版图（Steam 不提供海报尺寸）
- 优先使用 poster_url，fallback 到 cover_url
- 上传接口：保存文件 + 更新数据库
