# 阶段 8：平台基建补齐

> **状态**：📋 计划中

---

## 任务清单

| # | 任务 | 说明 | 优先度 |
|---|------|------|--------|
| 8.1 | 评论系统 | `ReviewController` + `ReviewSection.vue`，表 `reviews` 已建 | 高 |
| 8.2 | Redis 缓存 | `@Cacheable` 热门列表/推荐/搜索，TTL 10min | 高 |
| 8.3 | Swagger/SpringDoc | 添加 `springdoc-openapi` 依赖，自动生成 API 文档 | 中 |
| 8.4 | 定时抓取 | `@Scheduled` 每日凌晨 2 点自动拉取 Steam/TMDB | 中 |
| 8.5 | Docker 部署 | Dockerfile×2 + nginx.conf + docker-compose 整合 | 中 |
| 8.6 | 管理员上传竖版图 | `items` 表新增 `poster_url` 字段，`POST /api/admin/upload-poster/{id}`，前端卡片优先使用竖版图，fallback 横版 `cover_url` | 低 |

## 详细说明

### 8.1 评论系统
- 表 `reviews` 已有（item_id, user_id, content, rating, created_at）
- 需要 ReviewController (GET/POST /api/reviews)
- 前端 ReviewSection.vue 组件
- 支持评分 + 文字评论

### 8.2 Redis 缓存
- 热门列表：`items:hot`
- 推荐列表：`items:recommended`
- 搜索结果：`search:{query}`
- 使用 Spring `@Cacheable` 注解 + RedisTemplate

### 8.6 管理员上传竖版图
- GameCard 目前缺竖版图（Steam 不提供海报尺寸）
- 优先使用 poster_url，fallback 到 cover_url
- 上传接口：保存文件 + 更新数据库
