# Code Review Report — Commit 75cbf5d

**Review Date**: 2026-07-14
**Review Scope**: `HEAD~1..HEAD` (commit 75cbf5d)
**Effort Level**: Max (5+5 angles × 8 candidates → 1-vote verify → sweep)
**Files Reviewed**:
- `.github/workflows/ci-cd.yml` (new)
- `.github/workflows/ci.yml` (deleted)
- `.github/workflows/docker-build.yml` (deleted)
- `.github/workflows/deploy.yml` (deleted)
- `backend/src/test/java/com/example/demonet/controller/ReviewControllerTest.java`
- `backend/src/test/java/com/example/demonet/controller/UserItemControllerTest.java`
- `backend/src/test/java/com/example/demonet/service/IGDBServiceTest.java`
- `backend/src/main/java/com/example/demonet/controller/ReviewController.java`
- `backend/src/main/java/com/example/demonet/controller/UserItemController.java`
- `backend/src/main/java/com/example/demonet/service/IGDBService.java`

---

## Phase 1 — Verification Results

### Angle A: Line-by-Line Diff Scan
**Status**: Completed
**Findings**: 5 candidates identified

### Angle B: Removed-Behavior Auditor
**Status**: Completed
**Findings**: 5 candidates identified

### Angle C: Cross-File Tracer
**Status**: Completed
**Findings**: 8 candidates identified

### Angle D: Language-Pitfall Specialist
**Status**: Completed
**Findings**: 7 candidates identified

### Angle E: Wrapper/Proxy Correctness
**Status**: Completed
**Findings**: 4 candidates identified

### Reuse
**Status**: Completed
**Findings**: 8 candidates identified

### Simplification
**Status**: Completed
**Findings**: 8 candidates identified

### Efficiency
**Status**: Completed
**Findings**: 8 candidates identified

### Altitude
**Status**: Completed
**Findings**: 8 candidates identified

### Conventions (CLAUDE.md)
**Status**: No CLAUDE.md files found in repository

---

## Phase 2 — Verified Findings

| # | File | Line | Severity | Summary | Verdict |
|---|------|------|----------|---------|---------|
| 1 | ReviewController.java | 23, 41 | 🔴 High | Unsafe cast of `auth.getPrincipal()` to `Long` without type check | PLAUSIBLE |
| 2 | UserItemControllerTest.java | 110-112 | 🔴 High | Assertion changed from JSON structure to raw body string masks API regression | CONFIRMED |
| 3 | IGDBService.java | 218-220 | 🔴 High | Exception swallowed in `executeQuery` prevents distinguishing failures from empty results | CONFIRMED |
| 4 | IGDBServiceTest.java | 179-184 | 🟠 Medium | `lenient()` masks incomplete mock chain; stub IS consumed but leniency hides real issues | CONFIRMED |
| 5 | ci-cd.yml | 252-256 | 🟠 Medium | Frontend artifact downloaded but not validated for non-empty before Docker build | CONFIRMED |
| 6 | ReviewControllerTest.java | 39-41 | 🟡 Low | Duplicate `auth` mock setup code without shared test base class | PLAUSIBLE |
| 7 | IGDBService.java | 56-61 | 🟡 Low | Empty catch block in `refreshCredentialsIfNeeded` swallows database errors silently | PLAUSIBLE |
| 8 | IGDBServiceTest.java | 88-94, 120-124, 136-141, 161-166 | 🟢 Low | RestClient mock chain duplicated 4 times; should be extracted to @BeforeEach | PLAUSIBLE |
| 9 | AdminController.java | 751 | 🟢 Low | AdminController uses `Authentication` parameter but has no corresponding test | PLAUSIBLE |
| 10 | ci-cd.yml | 106, 190 | 🟢 Low | JDK installation duplicated in backend-build and backend-test jobs | PLAUSIBLE |

**Refuted Candidates**:
- ~~CSRF protection removed~~ → **REFUTED**: SecurityConfig.java:28 explicitly disables CSRF
- ~~Test isolation broken~~ → **REFUTED**: @BeforeEach creates fresh mock per test
- ~~workflow_dispatch removed~~ → **REFUTED**: ci-cd.yml:44 preserves manual trigger

---

## Phase 3 — Gap Analysis

**New findings not in verified list**:
- RedisTemplate stub scattered across multiple tests (IGDBServiceTest.java:53, 61, 110, etc.)
- Deploy SSH script lacks health check after `docker compose up`
- JWT_SECRET semantic weakening (removed "min-256bit" reminder)

These are lower-priority cleanup items and don't meet the bar for inclusion in the final report.

---

## Final Findings (JSON)

```json
[
  {
    "file": "backend/src/main/java/com/example/demonet/controller/ReviewController.java",
    "line": 23,
    "summary": "Unsafe cast of auth.getPrincipal() to Long without instanceof check",
    "failure_scenario": "If JWT decoder is reconfigured to return String principal instead of Long, all authenticated endpoints throw ClassCastException. Current JwtAuthFilter returns Long, but no type safety guard exists in Controller."
  },
  {
    "file": "backend/src/test/java/com/example/demonet/controller/UserItemControllerTest.java",
    "line": 112,
    "summary": "Assertion changed from jsonPath().isEmpty() to content().string('') masks API behavior regression",
    "failure_scenario": "When UserItemController.getOne() returns null (item not found), Spring serializes as empty body. New assertion passes but doesn't verify REST API design (should return 404, not 200 with empty body). If controller is fixed to return ResponseEntity with 404, this test breaks but provides unclear failure reason."
  },
  {
    "file": "backend/src/main/java/com/example/demonet/service/IGDBService.java",
    "line": 218,
    "summary": "Exception swallowed in executeQuery catch block prevents error propagation",
    "failure_scenario": "Callers cannot distinguish between 'API returned empty list' and 'API call failed'. RestClient timeout, JSON parse error, or network failure all return empty list. Debugging production issues requires correlating external logs instead of stack traces."
  },
  {
    "file": "backend/src/test/java/com/example/demonet/service/IGDBServiceTest.java",
    "line": 179,
    "summary": "lenient() used on all stubs masks incomplete mock chain verification",
    "failure_scenario": "fetchGameById_emptyResponse test uses lenient() on 6 stubs including .body(). While .body() IS consumed by actual code, lenient() prevents Mockito from detecting if future refactoring makes .body() unnecessary or adds new unchained calls (e.g., .headers()). Test would silently pass with wrong mock setup."
  },
  {
    "file": ".github/workflows/ci-cd.yml",
    "line": 256,
    "summary": "Frontend artifact downloaded but not validated for non-empty before Docker build",
    "failure_scenario": "If frontend-build job uploads empty artifact (e.g., pnpm build fails silently or dist/ is empty), deploy job downloads empty directory, Docker builds image with missing frontend, and production deployment serves broken SPA without clear CI failure."
  }
]
```

---

## Recommendations

### Immediate (P0)
1. **Add type guard in Controllers**: `if (!(auth.getPrincipal() instanceof Long)) throw new BadRequestException(...)`
2. **Revert getOne_notFound assertion**: Use `status().isNoContent()` or fix Controller to return 404
3. **Improve IGDBService error handling**: Log full exception with stack trace and throw domain exception

### High Priority (P1)
4. **Remove lenient() from IGDBServiceTest**: Verify stub chain is complete, or document why leniency is needed
5. **Add artifact validation**: After downloading frontend-dist, verify directory exists and contains index.html

### Medium Priority (P2)
6. **Extract shared test base class**: Reduce duplicate auth mock setup
7. **Add trace context to IGDBService logs**: Include query body in error logs for debugging
8. **Create AdminControllerTest**: Cover admin endpoints with authentication tests

### Low Priority (P3)
9. **Extract RestClient mock chain to @BeforeEach in IGDBServiceTest**
10. **Consolidate JDK installation** in ci-cd.yml (consider reusable workflows)

---

## Commit Quality Assessment

**Positive Changes**:
- ✅ Workflow consolidation reduces maintenance burden
- ✅ Added RabbitMQ service for integration tests
- ✅ Fixed IGDB body() stub chain (content is correct, wrapped in lenient)
- ✅ Proper test report archival with `if: always()`
- ✅ Deploy job requires both frontend-build and backend-test success

**Areas for Improvement**:
- ⚠️ Authentication approach oscillated 3+ times across commits (c181612 → 0c5337a → 1191e6b → 75cbf5d)
- ⚠️ lenient() masks potential test issues instead of fixing root cause
- ⚠️ Some production code quality issues (exception swallowing, type safety) not addressed by test fixes
- ⚠️ Missing test coverage for AdminController endpoints

**Overall Grade**: B- (Test infrastructure fixed, but production code quality issues remain)
