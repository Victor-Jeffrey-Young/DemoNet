# 问题与待办

---

## 一、已修复（阶段 9 完成项）

| # | 类别 | 问题 | 修复 |
|---|------|------|------|
| S1 | 安全 | SQL 拼接注入 | `JdbcTemplate` 参数化查询 |
| S2 | 安全 | Admin 路由无前端守卫 | 添加 `requiresAdmin` 守卫 |
| S3 | 安全 | Admin 接口无后端角色校验 | SecurityConfig `.hasRole("ADMIN")` |
| S4 | 安全 | 敏感信息硬编码 | 全部改为 `${ENV_VAR}` + `.env.example` |
| A1 | 架构 | 无全局异常处理 | GlobalExceptionHandler |
| A3 | 架构 | passwordHash 暴露 | 加 `@JsonIgnore` |
| B1 | Bug | 401/403 重定向失效 | axios 拦截器 + router beforeEach |
| B2 | Bug | Tailwind 动态类名不生效 | types.js 存储完整类名 |
| B4 | Bug | SearchBar 回车绕过防抖 | 回车立即搜索 + 图标居中 |

### 咖啡频道增强 (2026-06-24)

| # | 类别 | 问题 | 解决 |
|---|------|------|------|
| C1 | 技术 | ECharts 5 tree-shaking 砍掉雷达图 | 改用 `echarts/dist/echarts.esm.js` 完整包 |
| C2 | 技术 | v-if 内 ref 绑定延迟，nextTick 拿不到 DOM | 100ms 轮询重试（最多 20 次）等 ref 就绪 |
| C3 | 数据 | flavor 字段被风味描述字符串覆盖数值 | 重命名为 flavor_notes，flavor 保持数值 |

---

## 二、待修复

### 当前已知问题

| # | 问题 | 优先级 | 说明 |
|---|------|--------|------|
| T1 | EPUB 阅读器空白 | P1 | epub.js CDN/版本问题，待换 v0.4 或自建 |
| T2 | TMDB 网络不通 | P2 | 中国大陆环境需重试/代理机制 |
| #83 | Select 下拉白底（详情页编辑器） | P3 | Element Plus CSS 变量覆盖不彻底，不影响使用 |
| T3 | 游戏轮播卡片文字两行截断 | P3 | 浏览器缩放 125% 时 aspect-[21/10]+overflow-hidden 裁剪文字，多次尝试未修复 |

### 建议修复（代码审查）

| # | 类别 | 问题 | 方案 |
|---|------|------|------|
| A2 | 后端 | 无输入校验 | DTO 加 `@NotBlank/@Email/@Size`，Controller 加 `@Valid` |
| A4 | 后端 | Entity/DB 不同步风险 | 引入 Flyway 管理 schema 迁移 |
| A5 | 后端 | 推荐查询用原生 SQL | 改 MyBatis-Plus `selectPage` |
| B3 | 前端 | Element Plus 全量引入 | 按需引入（影响 ~1MB 体积） |
| F1 | 前端 | 类型元数据多处重复 | 统一从 `constants/types.js` 导入 |
| F2 | 前端 | 无数据层缓存 | Pinia `stores/item.js` |
| F3 | 前端 | 加载态简陋 | 骨架屏组件替代"加载中..." |
| F4 | 前端 | 空 catch 静默吞错 | 全局 Toast 错误提示 |

---

## 三、后续计划（阶段 10-13）

| 阶段 | 内容 | 状态 |
|------|------|------|
| 10 | **上线冲刺**：替换图片 + HTTPS + 测试 + Docker 部署 | 📋 |
| 11 | **互动体验**：评论增强 / 个人主页完善 / 点赞 / 分享 | 📋 |
| 12 | **发现推荐**：Elasticsearch 搜索 / CI/CD / 数据看板 | 📋 |
| 13 | **质量深化**：Flyway 迁移 / 输入校验 / 骨架屏 / EPUB 修复 | 📋 |

---

## 四、中期建议

| # | 建议 | 说明 |
|---|------|------|
| M1 | Redis 缓存 | ✅ 已完成（阶段 8） |
| M2 | 评论系统 | ✅ 已完成（阶段 8） |
| M3 | Swagger 文档 | ✅ 已完成（阶段 8） |
| M4 | 定时抓取 | ⏳ 框架就绪，待接入具体逻辑 |
| M5 | 单元测试 | 📋 当前 0 覆盖 |

---

## 五、长期建议

| # | 建议 | 说明 |
|---|------|------|
| L1 | Elasticsearch | 替换 `LIKE '%keyword%'` 全表扫描 |
| L2 | CI/CD | GitHub Actions 自动构建部署 |
| L3 | TypeScript 迁移 | 类型约束预防 typo/格式不一致 |
| L4 | 路径别名 | `@/` → `src/` |

---

## 优先级排序

```
安全隐患 > 前端 Bug > 后端架构 > 前端架构 > 中期基建 > 长期优化
```
